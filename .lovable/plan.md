

## Simplify Footer Navigation

Remove individual industry/use-case links from the footer and keep only top-level pages. The nav dropdown already handles use-case discovery.

### Changes

**`src/components/SiteFooter.tsx`** — Remove the Pharma and Financial Services links. Keep only: Blog, About, Privacy. This keeps the footer clean as more use-cases are added.

