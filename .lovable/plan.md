

## Add "Book a Demo" Button to Homepage

Add a "Book a demo" button to the homepage hero, alongside the existing "pharma clinical trials" link. Style it consistently with the pharma page's demo button (white rounded pill on dark background).

### Changes

**`src/pages/Index.tsx`** — Add a "Book a demo" button in the hero content area, after the "Starting with pharma clinical trials" text. Opens `https://calendar.app.google/J22JUbRhovodHYdk8` in a new tab. Styled as a white rounded-full button matching the existing design language.

